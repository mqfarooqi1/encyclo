"""AI provider abstraction. Optional by design: with no provider configured the
application runs in no-ai mode and every AI surface reports itself unavailable
rather than degrading into guesswork."""
